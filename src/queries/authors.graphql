fragment AuthorDetails on Author {
  name
  jobTitle
  biography
  url
  twitterUrl
  email
  picture {
    url
    width
    height
  }
}

query GetAuthorById($id: ID!) {
  author(where: { id: $id }) {
    ...AuthorDetails
  }
}
